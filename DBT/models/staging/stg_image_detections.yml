version: 2

models:
  - name: stg_image_detections
    columns:
      - name: message_id
        tests:
          - not_null
      - name: detected_object_class
        tests:
          - not_null
      - name: confidence_score
        tests:
          - not_null
